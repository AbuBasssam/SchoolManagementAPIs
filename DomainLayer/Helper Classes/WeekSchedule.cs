namespace DomainLayer.Helper_Classes
{
    public class WeekSchedule
    {
        #region var/prop(s)

        public bool SUN { get; set; }
        public bool MON { get; set; }
        public bool TUE { get; set; }
        public bool WED { get; set; }
        public bool THU { get; set; }
        //public List<string> GetActiveSchedule()
        //{
        //    List<string> ActiveSchedule = new List<string>();
        //    if (SUN)
        //        ActiveSchedule.Add(nameof(SUN));
        //    if (MON)
        //        ActiveSchedule.Add(nameof(MON));
        //    if (TUE)
        //        ActiveSchedule.Add(nameof(TUE));
        //    if (WED)
        //        ActiveSchedule.Add(nameof(WED));
        //    if (THU)
        //        ActiveSchedule.Add(nameof(THU));

        //    return ActiveSchedule;

        //}

        #endregion
    }
}
